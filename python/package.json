{
  "name": "python-server",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen": "poetry run python -m grpc_tools.protoc -I ../hellogrpc/proto/ --python_out=./python/autogen --pyi_out=./python/autogen --grpc_python_out=./python/autogen ../hellogrpc/proto/sample.proto"
  }
}
